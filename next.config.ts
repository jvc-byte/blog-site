import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['tailwindui.com', 'media-cdn.kroll.com', 'images.unsplash.com', 'gratisography.com'],
  },
};

export default nextConfig;
